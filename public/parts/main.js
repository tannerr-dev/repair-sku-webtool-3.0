
function backcover() {
		  // Get the text field
		  var copyText = document.getElementById("backcover");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function backcover2() {
			// Get the text field
			var copyText = document.getElementById("backcover2");
  
			// Select the text field
			copyText.select();
			copyText.setSelectionRange(0, 99999); // For mobile devices
  
			 // Copy the text inside the text field
			navigator.clipboard.writeText(copyText.value);
  
		  }
		function battery() {
		  // Get the text field
		  var copyText = document.getElementById("battery");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function bezel() {
		  // Get the text field
		  var copyText = document.getElementById("bezel");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function bottomCover() {
		  // Get the text field
		  var copyText = document.getElementById("bottomCover");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function bottomCover2() {
		  // Get the text field
		  var copyText = document.getElementById("bottomCover2");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function batterycable() {
		  // Get the text field
		  var copyText = document.getElementById("batterycable");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function camera() {
		  // Get the text field
		  var copyText = document.getElementById("camera");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function camera2() {
		  // Get the text field
		  var copyText = document.getElementById("camera2");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function cameracable() {
		  // Get the text field
		  var copyText = document.getElementById("cameracable");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function cameracable2() {
		  // Get the text field
		  var copyText = document.getElementById("cameracable2");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function lcdcable() {
		  // Get the text field
		  var copyText = document.getElementById("lcdcable");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function lcdcable2() {
		  // Get the text field
		  var copyText = document.getElementById("lcdcable2");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function sensorcable() {
		  // Get the text field
		  var copyText = document.getElementById("sensorcable");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function tpdcable() {
		  // Get the text field
		  var copyText = document.getElementById("tpdcable");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function dau() {
		  // Get the text field
		  var copyText = document.getElementById("dau");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}	
		function dau2() {
		  // Get the text field
		  var copyText = document.getElementById("dau2");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function dau3() {
		  // Get the text field
		  var copyText = document.getElementById("dau3");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function dau4() {
		  // Get the text field
		  var copyText = document.getElementById("dau4");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function sensor() {
		  // Get the text field
		  var copyText = document.getElementById("sensor");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}			
		function hng() {
		  // Get the text field
		  var copyText = document.getElementById("hng");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}			
		
		function kyb() {
		  // Get the text field
		  var copyText = document.getElementById("kyb");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}			
		function lcd() {
		  // Get the text field
		  var copyText = document.getElementById("lcd");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function lcd2() {
		  // Get the text field
		  var copyText = document.getElementById("lcd2");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
	
		function lcd3() {
		// Get the text field
		var copyText = document.getElementById("lcd3");

		// Select the text field
		copyText.select();
		copyText.setSelectionRange(0, 99999); // For mobile devices

		// Copy the text inside the text field
		navigator.clipboard.writeText(copyText.value);

		}
		function mic() {
		  // Get the text field
		  var copyText = document.getElementById("mic");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function mic2() {
		  // Get the text field
		  var copyText = document.getElementById("mic2");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}	
		function mot() {
		  // Get the text field
		  var copyText = document.getElementById("mot");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}	


		function mot2() {
		  // Get the text field
		  var copyText = document.getElementById("mot2");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function mot3() {
			// Get the text field
			var copyText = document.getElementById("mot3");
  
			// Select the text field
			copyText.select();
			copyText.setSelectionRange(0, 99999); // For mobile devices
  
			 // Copy the text inside the text field
			navigator.clipboard.writeText(copyText.value);
  
		  }	
		function plm() {
		  // Get the text field
		  var copyText = document.getElementById("plm");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}		
		function plm2() {
		  // Get the text field
		  var copyText = document.getElementById("plm2");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}	
		function spk() {
		  // Get the text field
		  var copyText = document.getElementById("spk");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}					
		function tpd() {
		  // Get the text field
		  var copyText = document.getElementById("tpd");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function trm() {
		  // Get the text field
		  var copyText = document.getElementById("trm");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function trm2() {
		  // Get the text field
		  var copyText = document.getElementById("trm2");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}
		function wifi() {
		  // Get the text field
		  var copyText = document.getElementById("wifi");

		  // Select the text field
		  copyText.select();
		  copyText.setSelectionRange(0, 99999); // For mobile devices

		   // Copy the text inside the text field
		  navigator.clipboard.writeText(copyText.value);

		}					
	
		function disc() {
		// Get the text field
		var copyText = document.getElementById("disc");

		// Select the text field
		copyText.select();
		copyText.setSelectionRange(0, 99999); // For mobile devices

		// Copy the text inside the text field
		navigator.clipboard.writeText(copyText.value);

		}			